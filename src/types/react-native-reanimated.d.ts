// Disabled temporary augmentation — left intentionally empty to avoid shadowing
// real types from 'react-native-reanimated'. If you still need to patch types,
// prefer adding a minimal augmentation that re-exports or narrows types instead.

export {};
